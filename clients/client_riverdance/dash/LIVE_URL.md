# Live URL -- template dashboard

The live dashboard URL goes here once deployed and its custom domain is mapped:

    https://template.agoradatadriven.com

(Not yet mapped.)

This volatile value lives in this README on purpose -- it changes per deploy/domain-map
and must NEVER be baked into `CLAUDE.md` (which is for fixed facts only). Update the line
above once `template.agoradatadriven.com` resolves to the `template-dash` Cloud Run service.
